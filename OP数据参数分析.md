# OP 数据 (上号 Token) 参数分析

在免密/代练上号圈子中，用于劫持腾讯开放平台（Tencent Open Platform）授权的 Token 数据通常被称为 **“OP 数据”**。
标准格式为使用竖线 `|` 分割的 5 段字符串。

## 示例数据
`1FD69915A8DA6583877FF15D43D316A3|52EBC9DD6296CD075C9B3F057B28873B|1107D9DE95ACBB9D1E344209B777804C|70124a5849d05be37b0af9ba3ad5a72c|1782303420`

---

## 参数逐一拆解

### 1. `openid` (用户唯一标识)
* **示例值：** `1FD69915A8DA6583877FF15D43D316A3` (32位 MD5 格式的 Hex 字符串)
* **作用：** 相当于该 QQ 号在当前游戏/应用中的“身份证号”。腾讯出于隐私保护，不会直接下发真实 QQ 号，而是针对每个接入的游戏生成一个独一无二的 `openid`，游戏端依赖此标识来区分账号体系。

### 2. `access_token` (登录访问令牌)
* **示例值：** `52EBC9DD6296CD075C9B3F057B28873B` (32位)
* **作用：** 核心“门票”。游戏 SDK 获取此 Token 后，会向腾讯服务器发起二次校验。仅当 Token 有效时，游戏才允许登录。通常具有有效期（如 90 天，代码中常硬编码 `expires_in: 7776000`）。

### 3. `pay_token` (支付令牌)
* **示例值：** `1107D9DE95ACBB9D1E344209B777804C` (32位)
* **作用：** 专门用于**游戏内充值（米大师支付系统）**的票据凭证。即使游戏成功登录，若要进行内购（如购买点券、皮肤），腾讯支付组件仍会校验此 `pay_token`。缺失此参数会导致无法充值。

### 4. `pfkey` (平台来源密钥)
* **示例值：** `70124a5849d05be37b0af9ba3ad5a72c` (32位)
* **作用：** 用于标记并校验用户的登录环境来源。在底层代码中常配合 `pf: "desktop_m_qq-10000144-android-2002-"` 使用，用于向腾讯服务器声明当前授权请求发生的具体渠道（如安卓端手Q）。

### 5. `auth_time` / `expires_time` (授权时间戳)
* **示例值：** `1782303420` (10位 Unix 时间戳)
* **换算时间：** 2026年06月24日 20:17:00
* **作用：** 记录该份 OP 数据被提取生成的确切时间。游戏 SDK 会结合此时间戳及过期策略，来判定当前授权数据是否已失效。

---

## 上号器核心原理总结

当在 OP 登录器界面粘贴数据并确认时，App 内部执行了如下核心逻辑（参考 `AgentActivity.java`）：

1. **数据切割**：将传入的字符串按 `|` 切割为 5 份数组。
2. **JSON 组装**：将上述 5 个参数分别对应 `openid`, `access_token`, `pay_token`, `pfkey`, `auth_time` 组装成一个 JSON 对象，并补充必要的常量（如 `ret: 0`）。
3. **Intent 回传**：将组装好的 JSON 放入 `Intent` 的 `key_response` 字段中。
4. **欺骗验证**：通过 `setResult(RESULT_OK, intent)` 将响应返回给正在等待授权的游戏（如王者荣耀/和平精英）。游戏接收到伪造的官方响应，校验通过后直接免密进入大厅。